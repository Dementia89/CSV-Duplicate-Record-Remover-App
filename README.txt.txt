The app is designed to use the file that is currently in the bin/Debug folder. It would be relatively
easy to rewrite it to accept a user input filepath. The output comes in the form of "output.csv", also
in the bin.Debug folder.